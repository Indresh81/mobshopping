{
  "name": "karina-kapoor-mobiles",
  "version": "1.0.0",
  "private": true,
  "description": "A small mobile phone store with a secure Node.js API and accessible frontend.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
